---
title: 'Basic CLI: Motoring Processes'
excerpt: 'How to monitor processes on the command line.'
category: 'snippet'
status: "draft"
tags:
  - 'Bash/Shell'
date: 'Apr 15, 2019'
---

- `ps` - displays the process that belongs to the cirrent user
  - `ps -ef` - displays everything runnning on the systrem
- `top` - real time proceess monitoring
- `kill` - allows you to send sginals to process based on their PIDs
  - to send a process signal, you must either be the owner of the process or be logged as the root user
- `pkill http*` - tool used to stop a process by using their name - for example the previous command will kill all the processes that start with `http`, such as `httpd` services fo the Apache Web Server
- `killall httpd` - tool used to stop a process by using their name - for example the previous command will kill all the processes that start with `http`, such as `httpd` services fo the Apache Web Server
- `killall -9 httpd` - tool used to stop a process by using their name - for example the previous command will kill all the processes that start with `http`, such as `httpd` services fo the Apache Web Server
